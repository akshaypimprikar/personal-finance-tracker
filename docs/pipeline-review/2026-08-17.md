---
date: 2026-08-17
addressed: true
---

# Pipeline Review — 2026-08-17

## Critical
- [x] `review.md` tells the agent to look for a "code-review round" section "from `/code-review`" — `/code-review` is not an invocable command/skill name; the file's own other references (lines 52, 88) correctly use `code-review:code-review`. This is a stale/incorrect skill reference that could cause an agent to look for the wrong marker in a PR body. — `.claude/commands/review.md:60` — **Fix:** replace `` `/code-review` `` with `` `code-review:code-review` `` to match the rest of the file. **Fixed** in `chore/2026-08-17-pipeline-review-findings`.

## High
(none found)

## Medium
- [x] `project_pragma_visibility_push.md` (and the matching line in `MEMORY.md`'s index) describes pragma PR #46 (v1.2.0) and FinanceTracker PR #92 (v1.2.2) as "both opened, awaiting merge before tag/back-merge/GitHub-release steps." Verified via `gh pr view 46 --repo akshaypimprikar/pragma` and `gh pr view 92 --repo akshaypimprikar/financetracker-ios`: both are now `MERGED` (merged 2026-08-17T05:41, essentially right after this memory was written). — `/Users/akshaypimprikar/.claude/projects/-Users-akshaypimprikar-Desktop-Claude-FinanceTracker/memory/project_pragma_visibility_push.md` — **Fix:** update the entry to reflect merged status and record whether the tag/back-merge/GitHub-release follow-ups for both releases have completed; refresh the corresponding `MEMORY.md` index line. **Fixed** — memory updated to reflect both releases fully closed out (tagged, back-merged, GitHub release published, feature-log PRs merged).
- [x] `project_rising_repos_experiment.md` (mtime 2026-06-12, 66 days old) set an explicit revisit point — "revisit after a few runs / after ~3-5 runs" — to decide whether to keep, promote, or remove the Rising Repos section from `agentic-ai-report.md` and `ios-engineering-report.md`. Both command files still read verbatim "Rising Repos (experimental — revisit after a few runs)" unchanged since the memory was written, and no later memory records that the review happened, despite these reports running routinely in the interim. — `/Users/akshaypimprikar/.claude/projects/-Users-akshaypimprikar-Desktop-Claude-FinanceTracker/memory/project_rising_repos_experiment.md` — **Fix:** actually run the keep/promote/remove assessment on recent Rising Repos output, or update the memory to note the review is still outstanding and why it's been deferred. **Partially addressed (2026-09-07)** — 3 real runs of both reports (2026-09-01, 09-03, 09-06) generated actual evidence: Agentic AI's Rising Repos surfaced a finding in 3/3 runs (`claude-mem`, `munder-difflin`, `NeoLabHQ/context-engineering-kit` + `Moadim.io`); iOS Engineering's surfaced nothing in 3/3 runs ("nothing qualifies" every time — GitHub's Swift trending list is dominated by macOS utility apps, not SwiftUI/SwiftData content). Recommendation given to Akshay: keep it in Agentic AI, drop/shrink it in iOS Engineering. Akshay's call: wait for a few more runs before deciding either way — checkpoint deliberately extended, not neglected.

## Low
- [x] pragma's `scripts/setup.sh` still generates a starter `CLAUDE.md` with `-destination 'platform=iOS Simulator,name=iPhone 17'` (no `OS=` pin) at lines 128/131/134. FinanceTracker fixed this exact ambiguity on 2026-08-16 (commit `2c09d19`, "pin ambiguous destination string") after more than one iOS runtime got installed locally, and ported the fix into the global `ios-build-verify`/`ios-coverage` skills — but the pragma scaffold generator, which is the one place that actually hardcodes a literal (non-templated) destination string for new projects, was missed. — `/Users/akshaypimprikar/Desktop/Claude/pragma/scripts/setup.sh:128,131,134` — **Fix:** add `,OS=<pin>` (or a comment telling the user to pin it) to the three generated `xcodebuild` destination strings, matching the guidance already in the global `ios-build-verify` skill. **Fixed** in pragma's `chore/2026-08-17-pipeline-review-findings` — used a `<pin to your installed runtime, e.g. 26.4.1>` placeholder rather than hardcoding FinanceTracker's own `26.4.1`, since this generator produces other people's projects' `CLAUDE.md`.
- [x] `.claude/settings.json`'s permissions allow-list has broader-than-needed and duplicated `Read` grants: `Read(//Users/akshaypimprikar/Desktop/**)` and `Read(//Users/akshaypimprikar/Documents/**)` grant blanket read access to entire home-directory subtrees far wider than the actual working set (`additionalDirectories` already lists the specific project folders needed: pragma, ActionTracker, Wiki, DevBrain). There are also redundant double-slash duplicates of narrower grants already covered by a broader pattern — e.g. `Read(/Users/akshaypimprikar/.claude/**)` and `Read(//Users/akshaypimprikar/.claude/**)` both present, and `Read(//Users/akshaypimprikar/.claude/skills/**)` / `Read(/Users/akshaypimprikar/.claude/plugins/**)` are already subsumed by the broader `.claude/**` grant. — `.claude/settings.json` (permissions.allow) — **Fix:** narrow the Desktop/Documents grants to the specific subfolders actually used, and dedupe the overlapping `.claude/**` variants. **Fixed** — removed all 4 `.claude/**` duplicate variants and the `Desktop/**` grant (redundant with `additionalDirectories`' explicit `Desktop` entry) in the original pass. `Documents/**` was left broad pending Akshay's input; now narrowed (2026-09-07) to `Documents/Resumes/**` and `Documents/Resumes 2025/**` — the one concretely evidenced use (resume/job-search work, tracked extensively elsewhere in memory) among the many unrelated folders and large archives (`Medidata Original Repos.zip`, old project folders) the blanket grant also covered.

## All clear
- Stale skill references (beyond the one Critical finding): every other `/command` and skill name across all 15 `.claude/commands/*.md` files resolves to a name in the current valid registry.
- Template drift — app-specific leaks: pragma's `.claude/commands/*.md` files are consistently genericized (`<AppName>`, `<Model>`, `<RepositoryProtocol>`-style placeholders); no hardcoded FinanceTracker paths, model names, or protocol names found.
- Template drift — missing template equivalents: every command file present in FinanceTracker's `.claude/commands/` has a counterpart in pragma's `.claude/commands/` (pragma additionally carries `init.md` and `pragma-review.md`, which are pragma-specific tooling with no FinanceTracker equivalent needed).
- CLAUDE.md token budget: 49 lines — within the ≤50 target.
- Pipeline gate coverage: all seven required gates/steps present and correctly wired — `/gates` exists (`.claude/commands/gates.md`), Gate 1 is build verification, `/feature` has a CHANGELOG step (line 21), `/review`-before-`/test` ordering is documented in both `feature.md`'s Done-when and `CLAUDE.md`'s pipeline description, `ios-coverage` is wired into Gate 6, `security-review` is wired into Gate 7, and `/status` exists for session recovery.
- Command file completeness: all 15 files in `.claude/commands/` have Trigger, Process, and Done when sections.
- Settings hygiene — hooks: both `PostToolUse` and `UserPromptSubmit` hooks are self-contained inline shell (no reference to missing external scripts); the pipeline-review alert hook is present and functioning (all four prior reports in `docs/pipeline-review/` are marked `addressed: true`, so no unaddressed backlog).
